

server:
    ssl:
        enabled: false # false = use http, true = use https (and the certificat)

spring:
    jpa:
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        generate-ddl: false # Set false for production
    datasource:
        url: jdbc:postgresql://host.docker.internal:5433/default
        username: tora